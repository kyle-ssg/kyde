[package]
name = "kyde-config"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Kyde config + persistence: keymap, language-pack list, recent projects. Pure, no GUI."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
